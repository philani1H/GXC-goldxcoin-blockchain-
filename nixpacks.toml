[phases.setup]
nixPkgs = ["python311", "cmake", "gcc"]

[phases.install]
cmds = ["python3 -m pip install --no-cache-dir -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python3 testnet_blockchain_node.py"
